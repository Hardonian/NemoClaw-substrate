🧹 Code Health Improvement: Refactor runSandboxDoctor

🎯 **What:** The `runSandboxDoctor` function in `src/lib/actions/sandbox/doctor.ts` was overly complex and long. It was refactored by extracting specific logical checks into their own smaller, well-named helper functions (`checkHostDockerDaemon`, `checkOpenShellCLI`, `checkGatewayOpenShellStatus`, `checkLiveSandbox`, `checkInferenceRoute`, `checkSandboxAgentAndShields`).

💡 **Why:** This improves maintainability and readability by significantly reducing the cyclomatic complexity of `runSandboxDoctor`, making it easier to understand, test, and modify the individual checks without wading through a massive function.

✅ **Verification:** I ran format and lint checks, ran the full test suite, and verified the code health issue was resolved. The same output behavior is strictly preserved.

✨ **Result:** A much cleaner, more modular `runSandboxDoctor` function that adheres to the single responsibility principle and is easier to maintain.
